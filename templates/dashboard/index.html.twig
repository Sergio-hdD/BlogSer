{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}
    Todos los posts
    {{dump(posts)}}
    Post por id
    {{ dump(postPorId) }}
    Post por título
    {{ dump(postPorTit) }}
    Posts con likes vacios
    {{ dump(postsPorLikes) }}
{% endblock %}
